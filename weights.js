let x1,x2,y1,y2;

function setup() {
    createCanvas(400, 600);
    angleMode(DEGREES); // Change the mode to DEGREES
    //frameRate(1);
    x1 = width/2;
    y1 = height*0.80;
    rope length.
}

//tegn kuler på snor,
//og glidere
function draw() {
  background(150);
  ellipse(x1);


}
